const timeUnits = new Map([['second', 1], ['minute', 60], ['hour', 3600]]);
console.log(timeUnits);
// o Array abaixo retorna o Map para o formato de Array de Array
console.log(Array.from(timeUnits));
