const circle = new Circle(10);
console.log(circle);
console.log(circle.area);
import Circle from './circle';

// a ordem da importação não importa.
// a importação e a exportação não pode estar dentro de bloco